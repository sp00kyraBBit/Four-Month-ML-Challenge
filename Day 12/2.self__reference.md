'def method(self)'....here self is needed to distinguish variables of different object. Otherwise these variables would have been floating around in every namespace of that class.So to make them attached to particular instances 'self' is used to access them through self either implicitely or explicitely.